version: 2.1
jobs:
  build:
    docker:
      - image: maloubobola/circleci-gcloud-node
    working_directory: ~/sweep
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
     
